internal class Horse     public bool available = true;     public string name;     public int distance;           public void Run(int moveDistance)     {           distance += moveDistance;     }class void Horse;static Random random;        static bool isGameFinished = false;        static int minSpeed;        static int maxSpeed;        static int finishDistance;의사코드1. GameObject 리스트에서 GameObject 5개를 초기화                              List<GameObject> 에 5개 초기화2. for문으로 GameObject 5 개의 각각 랜덤한 전진거리를 구한다                 for ( List<GameObject>.Count)                                                                                                moveDistance = Random(min, max)                                                                                                List[i].position += moveDistance;                                                                                                if (GameObject 위치 > 결승점 위치3. 전진 거리만큼 GameObject 5개를 각각 전진시킨다.                              for 문 끝                                                                                                                            if (GameObject 위치 > 결승점 위치등수별 리스트에 해당 GameObject 추가한다4. if ( GameObject 위치 > 결승점 위치 )                                                           등수별 리스트에 해당 GameObject 추가한다5. for 문 끝6. if ( 등수리스트 Count >= 5)             등수리스트[0].position = 1등 단상.position             등수리스트[1].position = 2등 단상.position             등수리스트[2].position = 3등 단상.position             